import SettingsClient from "@/components/modules/dashboard/SettingsClient";

export const metadata = {
  title: "Settings - Adivashindo",
};

export default function SettingsPage() {
  return <SettingsClient />;
}
